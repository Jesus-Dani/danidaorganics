# Copy to .env for local `netlify dev` testing. Never commit the real .env.
# In production these are set as Netlify environment variables (Site settings
# -> Environment variables), never hardcoded or committed.

JSONBIN_MASTER_KEY=
JSONBIN_BIN_ID=
ADMIN_PASSWORD=
SESSION_SECRET=
